language: ruby
rvm:
    — 2.7
services:
  - postgresql
addons:
  postgresql: "11.1"
apt:
  packages:
  - postgresql-11
  - postgresql-client-11
env:
  global:
    - PGPORT=5433
  - "RAILS_ENV=test"
before_script:
  - psql -c 'create database botsc-api_test;' -U postgres
  - RAILS_ENV=test rake db:migrate