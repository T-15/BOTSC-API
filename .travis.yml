language: ruby
rvm:
    — 2.7
env:
  global:
  - "RAILS_ENV=test"
before_script:
  - psql --version
  - RAILS_ENV=test rails db:create db:migrate
script: bundle exec rake spec
services:
  - postgresql